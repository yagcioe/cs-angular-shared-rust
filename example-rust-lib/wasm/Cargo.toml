[package]
name = "wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[package.metadata.wasm-pack.profile.release]
wasm-opt = true

[dependencies]
wasm-bindgen = "0.2"
"core" = { path = "../core" }